# VideoBox API - Railway

API para download de v¡deos hospedada no Railway.

## Endpoints:
- `/api/health` - Status da API
- `/api/process` - Processar URL de v¡deo
- `/api/status/<job_id>` - Status do processamento
- `/api/download/<job_id>/<filename>` - Download do arquivo

## Deploy:
- Conectado via GitHub
- Deploy autom tico no push